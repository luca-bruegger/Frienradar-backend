development:
  adapter: redis
  url: <%= ENV["REDIS_URL"] %>
  channel_prefix: frienradar_development

test:
  adapter: test

staging:
  adapter: redis-staging
  url: <%= ENV["REDIS_URL"] %>
  channel_prefix: frienradar_staging

production:
  adapter: redis-production
  url: <%= ENV["REDIS_URL"] %>
  channel_prefix: frienradar_production
