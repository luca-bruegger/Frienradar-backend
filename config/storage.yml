test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

 amazon_dev:
   service: S3
   access_key_id: ENV['S3_DEV_ACCESS_KEY']
   secret_access_key: ENV['S3_DEV_SECRET_KEY']
   region: eu-central-1
   bucket: arn:aws:s3:::frienradar-dev
